
[build]
  command = "npm run build"
  publish = ".next"

[dev]
  command = "next dev -p 9002"
  targetPort = 9002
  port = 8888

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["netlify/functions/utils/**"]
